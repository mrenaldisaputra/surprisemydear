{
  "name": "surprisemydear",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive birthday surprise website",
  "scripts": {
    "start": "npx serve .",
    "test": "echo \"No tests specified\" && exit 0"
  }
}
